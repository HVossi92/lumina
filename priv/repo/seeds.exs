# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# Production auto-seeding runs via Lumina.Release.seeds/0 (e.g. from the
# release startup script or bin/lumina eval "Lumina.Release.seeds").

Lumina.Release.seeds()
